package com.alejandro.habitjourney.core.data.local.enums

enum class HabitType {
    DO,
    QUANTITATIVE, // Lo vamos a quitar .. de momento se queda pero el user no puede elegirlo
    TIMER  // Lo vamos a quitar .. de momento se queda pero el user no puede elegirlo
}